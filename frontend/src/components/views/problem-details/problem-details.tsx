
type ProblemDetailsProps = {
    problemId: string
}

export const ProblemDetails = ({ problemId }: ProblemDetailsProps) => {
    return (
        <div>
            {problemId}
        </div>
    )
}
